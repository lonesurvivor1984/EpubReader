## Tasks

- [x] Fix action panel dismiss logic: mousedown handler checks `e.target.closest('.action-panel')` to prevent dismissing when clicking inside the panel. (reader.py line 566-570 — already applied)
- [x] Export button width: change `setFixedWidth(45)` to `setFixedWidth(65)` in settings.py line 102
