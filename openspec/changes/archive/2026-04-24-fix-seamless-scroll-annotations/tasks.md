## 1. Fix seamless scroll annotation restore

- [x] 1.1 Rewrite `restoreAnnotationsIn` in `_inject_seamless_scroll` to use `splitText` instead of `surroundContents`, matching the approach in `restore_annotations`
